$(function() {
// used to display product detail
  $('.single_product').click(function(){
    $(this).parent().find('div')[1].id
    let the_div = $(this).parent().find('div')[1].id
    let the_element = document.getElementById(the_div)
      console.log(the_element)
    if ( the_element.style.display == 'none' ) {
      the_element.style.display = 'block';
    } else {
      the_element.style.display = 'none';
    }
  });
});
